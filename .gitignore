MOCK UP# macOS metadata
.DS_Store
._*

# editors
.vscode/
.idea/

# node (if tooling is added later)
node_modules/

# local Cloudflare Worker dev state (wrangler dev --local)
worker/.wrangler/

# local secrets — never commit API keys
*.env
*.env.*
js/local.env.*
